# 宏观仪表盘 — neat-freak 知识收尾报告

**收尾时间**：2026-07-24
**收尾路径**：轻量路径（Python + Vercel 数据项目，文档齐全；最近 commit 全部为自动化数据更新，无 recent neat-freak 风格的"清理" commit）
**收尾者**：neat-freak（v3.0.0）

---

## 一、影响（用户视角）

- **🔴 暴露第四个命名不一致**：本地目录 `宏观仪表盘` ↔ GitHub remote `lj22503/macroview` ↔ 文档（README 第 1 行 `# 宏观仪表盘 (Macro Dashboard)` / CLAUDE.md 第 5 行 `宏观仪表盘 (Macro Dashboard)`）用中文名"宏观仪表盘"，remote 是英文 macroview。**累计 4 处命名错位**（idx 5 / 6 / 8 / 10）。
- **🔴 暴露违反项目核心规则**：CLAUDE.md 第 31 行明文规则 "**本地文件必须及时 commit**：`git reset --hard` 会丢弃未提交文件，每次修改重要文件后立即 commit"——但 git status 显示 `data/all_indicators.json` 与 `data/meta.json` 修改未提交。
- **暴露 .gitignore 末尾有 BOM/空白**：第 9-10 行有空白字符（可能是 Windows 编辑器写入），不影响功能但是 audit trail 噪音。
- **暴露 PROGRESS.md 可能未同步**：最近 5 个 commit 全是 `chore: 更新宏观数据 YYYY-MM-DD HH:MM` 自动化数据推送，PROGRESS.md（1.7KB）是否反映最新状态未读全文确认。

## 二、现役事实矩阵

| 事实面 | 状态 | 证据 |
|--------|------|------|
| 代码 | `verified-current` | backend/（FastAPI）+ frontend/（React + ECharts）+ scripts/（AKShare 数据源）+ src/；vercel.json 部署配置 |
| 运行态 | `changed-and-verified` | HEAD `95f0054`（2026-07-19 自动化数据更新）；Render + Vercel 双部署（README §技术架构）；数据每日本地拉 + push 到 GitHub Raw |
| 文档 | `verified-current` | CLAUDE.md 1.6KB（含字段命名规范 + 信号合成规则 + 重要约束）+ SPEC.md 12.6KB + PROGRESS.md 1.7KB + VERCEL_MIGRATION.md 2.5KB + README.md |
| 规则 | `verified-current` | CLAUDE.md 含开工流程 3 步 + 字段命名规范 + 4 条重要约束（含"本地必须及时 commit"）+ 4 条沟通原则 |
| 记忆 | `not-applicable` | 无 |
| 工作区 | `changed-and-verified` | 新建 `.neat-freak/`；2 项数据文件修改未提交（违反项目规则） |

## 三、关键发现

### 3.1 🔴 第四个命名不一致

| 维度 | 名字 | 证据 |
|------|------|------|
| 本地目录 | `宏观仪表盘`（中文） | `D:\claudework\宏观仪表盘\` |
| GitHub remote | `lj22503/macroview` | `git remote -v` |
| README.md | `宏观仪表盘 (Macro Dashboard)` | 第 1 行 |
| CLAUDE.md | `宏观仪表盘 (Macro Dashboard)` | 第 5 行 |
| queue.md 当前 idx | 10（项目名 `宏观仪表盘`） | queue.md |

→ 命名问题累计：
- idx 5（investment-advisory-skills / SoloAdvisor-Toolkit）
- idx 6（narrative-skill / diaolong-skill）
- idx 8（mangofolio / mangofolio-skill）
- idx 10（宏观仪表盘 / macroview）

### 3.2 🔴 违反 CLAUDE.md 核心规则

CLAUDE.md 第 31 行明文：
```
1. **本地文件必须及时 commit**：`git reset --hard` 会丢弃未提交文件，每次修改重要文件后立即 commit
```

但 git status 显示：
- ` M data/all_indicators.json`（修改未提交）
- ` M data/meta.json`（修改未提交）

→ 推测：本地拉数据的 AKShare 脚本刚跑完未 commit；按规则应立即 commit。
→ 该规则背景按 memory 2026-06-21 重大 git 事故：曾发生 `git reset --hard` 导致本地文件丢失。

### 3.3 .gitignore 末尾异常

| 行 | 内容 |
|----|------|
| 1-7 | 规则行 |
| 8 | `data/archives/` |
| 9-10 | 空白字符（推测 Windows 写入 BOM） |

→ 不影响功能，但是 audit trail 噪音。`__pycache__/` 和 `data/archives/` 都已覆盖。

### 3.4 数据自动化 commit 模式

git log 最近 5 个 commit：

```
95f0054 chore: 更新宏观数据 2026-07-19 09:01
227ea66 chore: 更新宏观数据 2026-07-18 09:01
f9b5abe chore: 更新宏观数据 2026-07-16 09:27
cbeaf62 chore: 更新宏观数据 2026-07-15 09:06
fb600ac chore: 更新宏观数据 2026-07-13 09:25
```

→ 自动化数据 cron 任务（推测 GitHub Actions）每天本地拉数据 → 自动 commit → push。
→ 本次 neat-freak commit 与 chore commit 节奏不同（不应当冲突）。

### 3.5 未跟踪内容（已被 .gitignore 忽略）

| 文件/目录 | 性质 | .gitignore 覆盖 |
|----------|------|---------------|
| `data/archives/` | 历史数据归档 | `data/archives/`（第 8 行）✓ |
| `scripts/__pycache__/` | Python 缓存 | `__pycache__/`（第 7 行）✓ |
| `scripts/source_adapters/__pycache__/` | Python 缓存 | `__pycache__/`（第 7 行）✓ |

→ 都是已 ignore 的，不算真实"未跟踪"问题。

## 四、改动 / 新建

| 文件 | 动作 | 原因 |
|------|------|------|
| `.neat-freak/reports/宏观仪表盘-2026-07-24.md` | 新建 | 本次 audit trail |

## 五、待你确认（未确认前不动作）

1. **🔴 命名不一致**：选项 A/B/C（见 queue.md 顶部"跨项目遗留"）+ 本项目特殊：本地是中文名，remote 是英文
2. **🔴 立即 commit `data/all_indicators.json` + `data/meta.json`**：违反 CLAUDE.md 第 31 行规则
3. **PROGRESS.md 是否需同步**：最近 5 个 chore commit 是否反映在 PROGRESS 里
4. **.gitignore 末尾空白字符**：是否清理
5. **VERCEL_MIGRATION.md（2.5KB）**：迁移报告是否仍最新（项目 Vercel 化时间点）

## 六、遗留

- SPEC.md 12.6KB 是大头，但本次仅看 CLAUDE.md（信号合成规则已提到 §5）
- docs/ 子目录内容未读
- VERCEL_MIGRATION.md 完整内容未读
- 5 因子信号（中美利差 / M1-M2 / VIX / 北向资金 / 信用利差）实际权重是否仍按 CLAUDE.md §26-27 行（20/25/15/15/25）运行未实测

---

*收尾完成度：5 事实面已标注（记忆 not-applicable）。报告基于 commit `95f0054`（HEAD，分支 main）。如需重新跑请清空 `.neat-freak/reports/` 后重跑。*